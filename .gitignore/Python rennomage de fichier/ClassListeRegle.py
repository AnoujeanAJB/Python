from ClassRègle.py import *
class LISTEREGLE:
    def __init__(self, règles=[]):
        self.règles=règles

    def Get_règles(self):
        règle
        for r in self.règles:
            règle=r.__str__()+"\n"
    def Set_règles(self, règle):
        self.règles.append(règle)
    def Charger(self, nomFichier):

    def Sauvegarder(slef, nomFichier):
        
